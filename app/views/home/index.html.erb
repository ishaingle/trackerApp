<div class="container">
		<% if user_signed_in? %>
			<p><%= link_to 'View Daily Status', daily_statuses_path %></p>
			<p><%= link_to 'View Tasks', tasks_path %></p>
			<p><%= link_to 'Logout', destroy_user_session_path, method: :delete, class: "btn btn-primary" %></p>
		<% else %>
			<p><%= link_to 'Login', new_user_session_path, class: "btn btn-primary" %></p>
		<% end %>
</div>
